#pragma once

const char PAGE[] PROGMEM = R"HTML(<!doctype html>
<html lang="de"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="theme-color" content="#18231f"><title>Diesel Heater · Gateway</title>
<style>
:root{color-scheme:light;--bg:#f3f2ec;--paper:#fffefa;--ink:#20312a;--muted:#6a756e;--line:#dcded4;--green:#315e48;--accent:#dca35b;--dark:#18231f}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--ink);font:15px/1.5 system-ui,-apple-system,BlinkMacSystemFont,"Segoe UI",sans-serif}button,input,select{font:inherit}button,a,input,select{touch-action:manipulation}button{cursor:pointer}button:disabled{cursor:not-allowed;opacity:.42}button:focus-visible,a:focus-visible,input:focus-visible,select:focus-visible{outline:3px solid #cc8a38;outline-offset:3px}button,.button{border:1px solid var(--line);border-radius:10px;padding:11px 17px;background:var(--paper);color:var(--ink);font-weight:600;text-decoration:none;display:inline-flex;align-items:center;justify-content:center;gap:8px;min-height:44px}.primary{background:var(--green);border-color:var(--green);color:white}.warm{background:#ebbb7d;border-color:#ebbb7d;color:#302211}.quiet{background:transparent}.small{padding:7px 12px;font-size:13px}h1,h2,h3,p{margin-top:0}h1{font-size:30px;line-height:1.2;letter-spacing:-1px;margin-bottom:8px}h2{font-size:19px;letter-spacing:-.3px;margin-bottom:8px}h3{font-size:15px;margin-bottom:6px}.muted{color:var(--muted)}.kicker{font-size:11px;text-transform:uppercase;letter-spacing:1.8px;font-weight:700}.shell{display:grid;grid-template-columns:228px minmax(0,1fr);min-height:100vh}.sidebar{background:var(--dark);color:#f4f4e9;padding:32px 22px;position:sticky;top:0;height:100vh;display:flex;flex-direction:column}.brand{display:flex;align-items:center;gap:12px;line-height:1.15;margin-bottom:52px}.brand b{font-size:18px;letter-spacing:-.5px}.brand small{color:#9daa9f;font-size:11px;letter-spacing:1.3px}.mark{width:37px;height:44px;color:#e8b77a;flex-shrink:0}.nav{display:flex;flex-direction:column;gap:7px}.nav button{border:0;background:transparent;justify-content:flex-start;color:#aebbb1;padding:13px 16px;font-weight:500}.nav button[aria-current=page]{color:#fff;background:#304338}.nav svg{width:19px;height:19px;fill:none;stroke:currentColor;stroke-width:1.6}.sidefoot{margin-top:auto;color:#9daa9f;font-size:12px}.sidefoot b{display:block;color:#e1e8df;margin:8px 0}.dot{display:inline-block;width:7px;height:7px;border-radius:50%;background:#82a98c;margin-right:7px}.dot.off{background:#cc9b68}.workspace{padding:28px 42px 38px;max-width:1500px;width:100%;margin:auto}.topbar{display:flex;justify-content:space-between;align-items:center;border-bottom:1px solid var(--line);padding-bottom:22px;margin-bottom:30px;gap:12px}.breadcrumb{font-size:12px;color:var(--muted)}.badge{display:inline-flex;align-items:center;border:1px solid var(--line);padding:5px 10px;border-radius:30px;font-size:12px;white-space:nowrap;background:#f9faf4}.badge.gold{background:#fcf1de;border-color:#e6ca9d;color:#755222}.heading{display:flex;justify-content:space-between;align-items:flex-start;gap:24px;margin-bottom:26px}.heading p{margin-bottom:0}.page[hidden],[hidden]{display:none!important}.grid{display:grid;gap:20px}.dashboard{grid-template-columns:minmax(0,1.4fr) minmax(285px,1fr)}.card{background:var(--paper);border:1px solid var(--line);border-radius:17px;padding:24px}.cardhead{display:flex;align-items:flex-start;justify-content:space-between;gap:12px;margin-bottom:18px}.cardhead p{font-size:13px;margin:0}.heater{background:var(--dark);border-color:var(--dark);color:#f8f5e9;overflow:hidden;position:relative}.heater .muted{color:#a8b5aa}.heater .badge{background:#29372e;border-color:#455045;color:#d9dfd1}.heater .badge.gold{color:#ffcf8d}.heater-title{font-size:16px}.dial{width:230px;height:230px;border-radius:50%;margin:10px auto 24px;display:flex;flex-direction:column;align-items:center;justify-content:center;border:1px solid #57634f;box-shadow:0 0 0 10px #263329,0 0 0 11px #384538;position:relative;background:radial-gradient(circle at 50% 90%,#354634 0,transparent 65%)}.dial:before{content:"";position:absolute;inset:-12px;border:3px solid transparent;border-top-color:#d9af72;border-radius:50%;transform:rotate(-35deg)}.temperature{font-size:66px;letter-spacing:-3px;font-weight:300;line-height:1.2}.temperature small{font-size:24px;letter-spacing:0;color:#c2c9b9}.dial .kicker{font-size:10px;color:#b4bda8;margin-bottom:10px}.dial p{font-size:12px;color:#bec7b4;margin:5px 0}.heater-note{margin:0;font-size:13px;text-align:center;color:#bdc7b8}.metricgrid{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:12px;margin-top:20px}.metric{padding:18px 14px;background:var(--paper);border:1px solid var(--line);border-radius:13px}.metric span{display:block;font-size:12px;color:var(--muted);margin-bottom:8px}.metric strong{font-weight:500;font-size:22px;white-space:nowrap}.controlcard{display:flex;flex-direction:column}.segmented{display:flex;padding:4px;border:1px solid var(--line);border-radius:11px;background:#f1f3ec;gap:4px;margin:12px 0 25px}.segmented button{flex:1;background:none;border:0;padding:9px;font-size:13px;min-height:40px}.segmented button[aria-pressed=true]{background:var(--paper);box-shadow:0 1px 4px #14221516}.stepper{display:flex;align-items:center;justify-content:center;gap:22px;margin:22px 0 28px}.stepper button{width:46px;height:46px;font-size:25px;font-weight:400}.setpoint{font-size:45px;letter-spacing:-1.5px;min-width:92px;text-align:center}.setpoint small{font-size:20px;letter-spacing:0}.controlcard>.primary,.controlcard>.warm{width:100%}.info{padding:13px 15px;border-radius:10px;background:#f0f3eb;font-size:13px;margin:18px 0 0}.demo-switch{display:flex;align-items:center;gap:10px;font-size:13px;cursor:pointer;margin-top:auto;padding-top:24px}.demo-switch input{width:18px;height:18px;accent-color:var(--green)}.summary{grid-template-columns:1fr 1fr;margin-top:20px}.listrow{display:flex;justify-content:space-between;align-items:center;gap:16px;padding:13px 0;border-bottom:1px solid #eaebe3;font-size:13px}.listrow:last-child{border:0;padding-bottom:0}.listrow strong{font-weight:600;text-align:right;overflow-wrap:anywhere}.status-text{font-size:12px}.good{color:#3f7750}.banner{border:1px solid #e3c393;background:#fbebce;color:#725024;padding:12px 16px;border-radius:11px;margin-bottom:20px;font-size:13px;display:flex;justify-content:space-between;align-items:center;gap:12px}.errorbanner{background:#fbe8e2;border-color:#e5b7a8;color:#833d2c}.notice{margin-top:18px;font-size:12px;color:var(--muted)}.toolbar{display:flex;align-items:center;justify-content:space-between;gap:12px;flex-wrap:wrap;margin:18px 0}.search{max-width:350px;flex:1;min-width:170px}input:not([type=checkbox]),select{border:1px solid #cbd1c4;border-radius:9px;background:#fffefb;color:var(--ink);padding:11px 12px;width:100%;min-height:44px}label.field{display:block;font-size:13px;font-weight:600;margin:20px 0 7px}.check{display:flex;align-items:center;gap:9px;font-size:13px;cursor:pointer}.check input{accent-color:var(--green);width:17px;height:17px}.devices{display:flex;flex-direction:column}.device{display:grid;grid-template-columns:minmax(0,1fr) auto;gap:16px;padding:18px 0;border-top:1px solid var(--line);align-items:center}.device h3{margin:0;overflow-wrap:anywhere}.device p{font-size:12px;color:var(--muted);margin:5px 0 0;overflow-wrap:anywhere}.device .badge{margin-top:8px}.device details{font-size:12px;color:var(--muted);margin-top:8px}.device summary{cursor:pointer}.device code{display:block;overflow-wrap:anywhere;white-space:normal}.empty{text-align:center;padding:38px 18px;color:var(--muted)}.empty b{display:block;color:var(--ink);margin-bottom:8px;font-size:17px}.empty p{max-width:470px;margin:auto;font-size:13px}.twocol{grid-template-columns:1.1fr 1fr}.stack{display:flex;flex-direction:column;gap:20px}.network{display:flex;justify-content:space-between;align-items:center;gap:16px;border-top:1px solid var(--line);padding:14px 0}.network div{min-width:0}.network strong{font-size:13px;overflow-wrap:anywhere}.network p{font-size:12px;margin:3px 0 0;color:var(--muted)}.actions{display:flex;align-items:center;gap:10px;flex-wrap:wrap;margin-top:22px}.events{list-style:none;padding:0;margin:0;max-height:270px;overflow:auto}.events li{display:grid;grid-template-columns:65px 1fr;gap:14px;border-bottom:1px solid var(--line);padding:12px 0;font-size:12px}.events time{color:var(--muted);font-variant-numeric:tabular-nums}.footer{display:flex;justify-content:space-between;gap:15px;color:var(--muted);font-size:11px;padding:26px 2px 0}.toast{position:fixed;bottom:24px;left:50%;transform:translateX(-50%);max-width:min(90vw,620px);padding:14px 22px;background:#233c2d;color:white;border:1px solid #667c68;border-radius:12px;box-shadow:0 8px 28px #0002;z-index:20;font-size:14px}.toast.error{background:#713e31;border-color:#a57461}dialog{border:1px solid var(--line);border-radius:18px;padding:28px;max-width:450px;width:calc(100% - 32px);color:var(--ink);background:var(--paper);box-shadow:0 18px 80px #0003}dialog::backdrop{background:#18231f88}dialog p{font-size:14px;white-space:pre-line}.screenreader{position:absolute;width:1px;height:1px;overflow:hidden;clip:rect(0,0,0,0)}
@media(min-width:1450px){.workspace{padding-left:60px;padding-right:60px}}@media(max-width:1050px){.shell{grid-template-columns:190px 1fr}.sidebar{padding:28px 16px}.workspace{padding:24px}.dashboard{grid-template-columns:1fr 1fr}.dial{width:195px;height:195px}.card{padding:20px}.metric strong{font-size:19px}.metric{padding:14px 10px}}@media(max-width:780px){.shell{display:block}.sidebar{position:static;height:auto;padding:18px 20px}.brand{margin-bottom:18px}.brand .mark{width:28px;height:32px}.brand b{font-size:17px}.brand small{display:none}.nav{flex-direction:row;gap:5px;justify-content:space-between}.nav button{padding:10px;font-size:12px;flex:1;justify-content:center}.nav svg{display:none}.sidefoot{display:none}.workspace{padding:22px 18px}.topbar{padding-bottom:17px;margin-bottom:24px}.dashboard,.summary,.twocol{grid-template-columns:1fr}.heading{gap:14px}h1{font-size:27px}.heading>.badge{display:none}.dial{width:215px;height:215px;margin-top:15px;margin-bottom:28px}.demo-switch{margin-top:20px}.card{padding:21px}.metric{padding:15px 12px}.metric strong{font-size:22px}.controlcard .stepper{margin:16px 0}.device{gap:10px}.device>button{font-size:12px;padding:9px 11px}.footer{flex-direction:column;gap:4px}.breadcrumb{font-size:11px}.banner{align-items:flex-start}.banner button{flex-shrink:0}.topbar .badge{font-size:11px}.toolbar .check{width:100%}}
@media(max-width:780px){.nav{gap:3px}.nav button{padding:10px 6px;font-size:11.5px}.nav{flex-wrap:wrap}#page-mqtt code{overflow-wrap:anywhere}#page-mqtt .listrow strong{max-width:60%}}
</style></head><body>
<div class="shell"><aside class="sidebar"><div class="brand"><svg class="mark" viewBox="0 0 40 48" aria-hidden="true"><path fill="currentColor" d="M23 1C25 15 8 16 10 30c-5-3-5-8-4-12C-7 35 4 47 20 47c17 0 28-22 10-34 2 11-3 14-7 16C28 17 19 16 23 1Z"/></svg><div><b>DIESEL HEATER</b><br><small>LOCAL GATEWAY</small></div></div>
<nav class="nav" aria-label="Hauptnavigation"><button data-page="home" aria-current="page"><svg viewBox="0 0 24 24"><path d="m3 10 9-7 9 7v11H3ZM9 21v-8h6v8"/></svg>Übersicht</button><button data-page="devices"><svg viewBox="0 0 24 24"><path d="m7 5 11 12-6 5V2l6 5L7 19"/></svg>Bluetooth</button><button data-page="wifi"><svg viewBox="0 0 24 24"><path d="M2 8a16 16 0 0 1 20 0M5 12a11 11 0 0 1 14 0M8 16a6 6 0 0 1 8 0"/><circle cx="12" cy="20" r="1"/></svg>WLAN</button><button data-page="mqtt"><svg viewBox="0 0 24 24"><path d="M4 17v-3m5 3V9m5 8V5m5 12V2"/></svg>MQTT</button><button data-page="updates"><svg viewBox="0 0 24 24"><path d="M12 3v12m-5-5 5 5 5-5M4 17v4h16v-4"/></svg>Updates</button><button data-page="system"><svg viewBox="0 0 24 24"><rect x="5" y="5" width="14" height="14" rx="3"/><path d="M9 1v4m6-4v4M9 19v4m6-4v4M1 9h4m-4 6h4m14-6h4m-4 6h4"/></svg>System</button></nav>
<div class="sidefoot"><span class="dot" id="side-dot"></span><span id="side-status">Verbindung wird geprüft</span><b>XIAO ESP32-S3</b>Firmware <span data-version>—</span><p style="margin:18px 0 0">Lokal verbunden.<br>Für unterwegs gemacht.</p></div></aside>
<main class="workspace"><header class="topbar"><span class="breadcrumb">MEIN GATEWAY <span style="margin:0 10px">/</span> <span id="crumb">Übersicht</span></span><span class="badge"><span class="dot" id="net-dot"></span><span id="net-status">Verbinde …</span></span></header>
<div class="banner errorbanner" id="offline" role="status" hidden>Gateway nicht erreichbar. Die Verbindung wird automatisch erneut geprüft. Echte Steuerung ist gesperrt.</div>
<div class="banner" id="demo-banner" hidden><span><strong>Demo aktiv · Beispieldaten</strong><br>Bedienung nur in diesem Browser. Es werden keine Heizungsbefehle gesendet.</span><button class="small quiet" id="exit-demo">Demo beenden</button></div>
<section class="page" id="page-home"><div class="heading"><div><p class="kicker muted">Dein Wärmeplatz</p><h1 id="gateway-name">Meine Dieselheizung</h1><p class="muted">Alles Wichtige auf einen Blick.</p></div><span class="badge" id="heater-badge">Heizung nicht verbunden</span></div>
<div class="grid dashboard"><div><article class="card heater"><div class="cardhead"><div><h2 class="heater-title">Heizungsstatus</h2><p class="muted" id="heater-subtitle">Warte auf die erste Verbindung</p></div><span class="badge" id="run-state">Nicht verbunden</span></div><div class="dial"><span class="kicker">Raumtemperatur</span><div class="temperature"><span id="room-temp">—</span><small> °C</small></div><p id="dial-note">Noch keine Messwerte</p></div><p class="heater-note" id="heater-note">Richte dein Gateway ein oder erkunde die Demo.</p></article><div class="metricgrid"><div class="metric"><span>Versorgung</span><strong id="voltage">— V</strong></div><div class="metric"><span>Gehäuse</span><strong id="body-temp">— °C</strong></div><div class="metric"><span>Leistung</span><strong id="power-level">— / 10</strong></div></div></div>
<article class="card controlcard"><div><p class="kicker muted">Wärme einstellen</p><h2>Deine Wohlfühltemperatur</h2><p class="muted" style="font-size:13px">Temperatur, Heizleistung oder Lüften ohne Heizen.</p></div><div class="segmented" aria-label="Betriebsart"><button id="mode-temp" aria-pressed="true" disabled>Temperatur</button><button id="mode-level" aria-pressed="false" disabled>Leistung</button><button id="mode-fan" aria-pressed="false" disabled>Lüften</button></div><span class="kicker muted" id="setpoint-label" style="text-align:center">Solltemperatur</span><div class="stepper"><button id="minus" aria-label="Sollwert verringern" disabled>−</button><div class="setpoint"><span id="setpoint">—</span><small id="setpoint-unit"> °C</small></div><button id="plus" aria-label="Sollwert erhöhen" disabled>+</button></div><button class="primary" id="power" disabled>Heizung nicht verbunden</button><p class="info" id="control-info">Heizungssteuerung noch nicht verfügbar. Lüften benötigt ein unterstütztes Steuergerät und einen geprüften Treiber. In der Demo kannst du den Modus ausprobieren.</p><label class="demo-switch"><input id="demo-toggle" type="checkbox">Oberfläche mit Beispieldaten ausprobieren</label></article></div>
<div class="grid summary"><article class="card"><div class="cardhead"><h2>Verbindungen</h2><span class="kicker muted">Live</span></div><div class="listrow"><span>WLAN</span><strong id="home-wifi">Wird geprüft …</strong></div><div class="listrow"><span>Bluetooth-Heizung</span><strong id="home-heater">Nicht ausgewählt</strong></div><div class="listrow"><span>MQTT</span><strong id="home-mqtt" class="muted">Deaktiviert</strong></div><div class="listrow"><span>Weboberfläche</span><strong id="home-web">Wird geprüft …</strong></div></article><article class="card"><p class="kicker muted">Nächster Schritt</p><h2 id="next-title">Deine Heizung hinzufügen</h2><p class="muted" id="next-text">Wenn die Heizung in Reichweite ist: mit Strom versorgen, die Hersteller-App schließen und nach Bluetooth-Geräten suchen.</p><button class="quiet" data-go="devices">Zur Gerätesuche <span aria-hidden="true">↗</span></button></article></div></section>
<section class="page" id="page-devices" hidden><div class="heading"><div><p class="kicker muted">Mit deiner Heizung verbinden</p><h1>Bluetooth-Geräte</h1><p class="muted">Suchen, erkennen und für später speichern.</p></div><button class="primary" id="scan" disabled>Geräte suchen</button></div><article class="card" id="saved-card" hidden><div class="cardhead"><div><p class="kicker muted">Gespeicherte Auswahl</p><h2 id="saved-name">—</h2><p id="saved-address"></p></div><button class="small quiet" id="forget">Auswahl aufheben</button></div><p class="info" id="saved-hint"></p><form id="heater-form"><label class="field" for="heater-profile">Heizungsprotokoll</label><select id="heater-profile"><option value="">Bitte passend zum Steuergerät auswählen</option><option value="0">AA55</option><option value="1">AA55 · verschlüsselt</option><option value="2">AA66</option><option value="3">AA66 · verschlüsselt</option><option value="4">ABBA / HeaterCC</option><option value="5">CBFF / Sunster</option><option value="6">Hcalory MVP1</option><option value="7">Hcalory MVP2</option></select><label class="field" for="heater-pin">Geräte-PIN (vier Ziffern)</label><input id="heater-pin" type="password" inputmode="numeric" pattern="[0-9]{4}" maxlength="4" autocomplete="new-password" placeholder="z. B. 1234 oder 0000"><label class="check" style="margin-top:12px"><input id="heater-keep-pin" type="checkbox">Gespeicherte PIN beibehalten</label><div class="actions"><button class="primary" id="heater-save">Profil & PIN speichern</button><button type="button" id="heater-connect">Verbinden</button><button type="button" id="heater-disconnect">Trennen</button></div></form><p class="info" id="heater-link-status">Nicht verbunden</p><p class="notice">Die PIN ist die der Heizung. Ein Bluetooth-Hinweis allein bestimmt das Protokoll nicht eindeutig. Beim Verbinden werden Anmeldung und Statusabfragen gesendet; die Heizung wird dabei nicht gestartet. Nach Neustart oder Verbindungsabbruch bitte erneut verbinden.</p></article><article class="card" style="margin-top:20px"><div class="cardhead"><div><h2>Geräte in der Nähe</h2><p class="muted" id="scan-status">Noch keine Suche durchgeführt.</p></div><span class="badge" id="device-count">0 Geräte</span></div><div class="toolbar"><input class="search" id="search" type="search" placeholder="Name oder Adresse suchen" aria-label="Geräte durchsuchen"><label class="check"><input type="checkbox" id="only-heaters">Nur mögliche Heizungen</label></div><div class="devices" id="devices"></div><p class="notice">Ein Protokollhinweis anhand der Service-UUID ist noch keine bestätigte Erkennung. Geräte ohne bekannte UUID können ebenfalls Heizungen sein. Die Auswahl allein verbindet noch nicht. Wähle anschließend das passende Profil und klicke auf Verbinden.</p></article></section>
<section class="page" id="page-wifi" hidden><div class="heading"><div><p class="kicker muted">Zuhause & unterwegs</p><h1>WLAN einrichten</h1><p class="muted">Dein Gateway bleibt auch ohne Internet bedienbar.</p></div></div><div class="grid twocol"><article class="card"><div class="cardhead"><div><h2>Netzwerkzugang</h2><p class="muted" id="wifi-current">Verbindung wird geprüft …</p></div></div><form id="wifi-form"><label class="field" for="ssid">WLAN-Name (2,4 GHz)</label><input id="ssid" maxlength="32" required autocomplete="off"><label class="field" for="password">WLAN-Passwort</label><input id="password" type="password" maxlength="63" autocomplete="new-password" placeholder="Passwort eingeben"><label class="check" style="margin-top:14px"><input id="keep-password" type="checkbox">Gespeichertes Passwort beibehalten</label><label class="check" style="margin-top:12px"><input id="open-network" type="checkbox">Dieses WLAN hat kein Passwort</label><div class="actions"><button class="primary" id="wifi-save" disabled>Speichern & neu starten</button></div></form><p class="notice">Nach dem Neustart: dieselheater.local oder die neue IP-Adresse öffnen. Bei fehlender Verbindung kehrt der Setup-Hotspot nach 30 Sekunden zurück.</p></article><div class="stack"><article class="card"><div class="cardhead"><h2>Verfügbare Netzwerke</h2><button class="small" id="wifi-scan" disabled>Suchen</button></div><div id="networks"><p class="muted" style="font-size:13px">Suche nach WLANs oder trage den Namen direkt ein.</p></div></article><article class="card"><h2>Setup-Hotspot</h2><div class="listrow"><span>Status</span><strong id="ap-state">—</strong></div><div class="listrow"><span>WLAN-Name</span><strong>DieselHeater-Setup</strong></div><div class="listrow"><span>Adresse</span><strong>192.168.4.1</strong></div><p class="notice">Der Hotspot schaltet sich eine Minute nach erfolgreicher WLAN-Verbindung ab. Setup-Passwort: dieselheater.</p></article></div></div></section>
<section class="page" id="page-mqtt" hidden><div class="heading"><div><p class="kicker muted">Mit Home Assistant verbinden</p><h1>MQTT & Smart Home</h1><p class="muted">Einmal einrichten. Automatisch in Home Assistant erkennen.</p></div><span class="badge" id="mqtt-badge">Deaktiviert</span></div>
<div class="grid twocol"><article class="card"><h2>Broker-Verbindung</h2><p class="muted" style="font-size:13px">Verwende denselben MQTT-Broker wie in Home Assistant, zum Beispiel deinen Mosquitto-Broker.</p><form id="mqtt-form"><label class="check" style="margin:20px 0"><input id="mqtt-enabled" type="checkbox">MQTT aktivieren</label><label class="field" for="mqtt-host">Broker-Adresse</label><input id="mqtt-host" maxlength="128" placeholder="z. B. 192.168.178.50" autocomplete="off"><label class="field" for="mqtt-port">Port</label><input id="mqtt-port" type="number" min="1" max="65535" value="1883" required><label class="field" for="mqtt-user">Benutzername (optional)</label><input id="mqtt-user" maxlength="64" autocomplete="off"><label class="field" for="mqtt-password">Passwort (optional)</label><input id="mqtt-password" type="password" maxlength="128" autocomplete="new-password"><label class="check" style="margin-top:14px"><input id="mqtt-keep" type="checkbox">Gespeichertes MQTT-Passwort beibehalten</label><label class="field" for="mqtt-interval">Status senden alle … Sekunden</label><input id="mqtt-interval" type="number" min="10" max="300" value="30" required><label class="check" style="margin-top:20px"><input id="mqtt-discovery" type="checkbox" checked>Home-Assistant-Erkennung aktivieren</label><div class="actions"><button class="primary" id="mqtt-save" disabled>MQTT speichern</button></div></form><p class="notice">Änderungen werden ohne Neustart übernommen. Lokales MQTT über TCP ohne TLS. Passwort und Benutzername werden an den hier eingetragenen Broker gesendet.</p></article>
<div class="stack"><article class="card"><div class="cardhead"><h2>Verbindungsstatus</h2><span class="badge" id="mqtt-connection">Deaktiviert</span></div><p class="info" id="mqtt-error" hidden></p><div class="listrow"><span>Nachrichten gesendet</span><strong id="mqtt-sent">0</strong></div><div class="listrow"><span>Vom Broker bestätigt</span><strong id="mqtt-acks">0</strong></div><div class="listrow"><span>Befehle empfangen</span><strong id="mqtt-received">0</strong></div><div class="listrow"><span>HA-Konfiguration bestätigt</span><strong id="mqtt-discovered">—</strong></div><p class="notice" id="mqtt-result">Noch kein Befehl empfangen.</p><div class="actions"><button class="small" id="mqtt-send" disabled>Status senden</button><button class="small" id="mqtt-discover" disabled>Discovery senden</button></div></article>
<article class="card"><p class="kicker muted">Automatische Einrichtung</p><h2>So erscheint das Gateway in HA</h2><p class="muted" style="font-size:13px">Die MQTT-Integration in Home Assistant muss mit demselben Broker verbunden sein. Danach legt MQTT Discovery das Gerät mit zwölf Einträgen an. Standard-Präfix: <code>homeassistant</code>.</p><div class="listrow"><span>Gateway-Diagnose</span><strong>WLAN, Laufzeit, Speicher</strong></div><div class="listrow"><span>Gateway bedienen</span><strong>Status abrufen, Neustart</strong></div><div class="listrow"><span>Heizungsregler & Messwerte</span><strong>Noch nicht verfügbar</strong></div><p class="info">Ohne Heizungs-Protokolltreiber bleiben Thermostat, Betriebsart, Leistungsstufe und Heizungswerte in HA nicht verfügbar. Die Browser-Demo wird niemals über MQTT gesendet.</p></article>
<article class="card"><h2>Topics & letzter Befehl</h2><p class="muted" style="font-size:13px">Feste, eindeutige Topics verhindern Überschneidungen mit weiteren Gateways.</p><label class="field" for="mqtt-topic">Basis-Topic</label><input id="mqtt-topic" readonly aria-label="MQTT Basis-Topic"><details style="margin-top:18px;font-size:13px"><summary style="cursor:pointer">Topics und Befehle anzeigen</summary><p class="notice">An das Basis-Topic anhängen:</p><p><code>/state</code> · Gateway-Status<br><code>/availability</code> · online/offline<br><code>/heater/state</code> · Live-Heizungswerte<br><code>/result</code> · Befehlsbestätigung</p><p><code>/command/refresh</code> → PRESS<br><code>/command/restart</code> → PRESS<br><code>/command/scan</code> → PRESS<br><code>/command/power</code> → ON / OFF<br><code>/command/mode</code> → heat / off / fan_only<br><code>/command/temperature</code> → 8–35<br><code>/command/level</code> → 1–10<br><code>/command/operating_mode</code> → temperature / level / ventilation</p><p class="notice">Befehle ohne Retain senden. Alte retained Befehle werden beim Wiederverbinden verworfen. Lüften wird in Home Assistant erst bei bestätigter Geräteunterstützung angeboten; sonst lautet die Antwort <code>ventilation_not_supported</code>. Ohne aktuelle Heizungsantwort bleiben die Regler gesperrt. <code>/heater/result</code> meldet die Rücklesebestätigung oder einen Fehler; eine Annahme ist noch keine Bestätigung.</p></details></article></div></div></section>
<section class="page" id="page-updates" hidden><div class="heading"><div><p class="kicker muted">Dein Gateway aktuell halten</p><h1>Firmware-Updates</h1><p class="muted">Neue Versionen aus deinem GitHub-Projekt oder direkt aus einer Datei.</p></div><span class="badge" data-version>—</span></div><div class="grid twocol"><article class="card"><p class="kicker muted">Aus GitHub</p><h2>Nach neuer Version suchen</h2><p class="muted">Geprüft wird die letzte stabile Veröffentlichung im gespeicherten öffentlichen Repository. Installiert wird erst nach deiner Bestätigung.</p><form id="update-repo-form"><label class="field" for="update-repo">GitHub-Repository</label><input id="update-repo" placeholder="Benutzer/Projekt" maxlength="180" autocomplete="off"><div class="actions"><button id="update-repo-save" type="submit">Repository speichern</button><button id="update-check" type="button" class="primary" disabled>Nach Updates suchen</button></div></form><p class="info" id="update-release-status" role="status">Noch nicht nach Updates gesucht.</p><div id="update-release" hidden><h3 id="update-release-title" style="margin-top:20px"></h3><p id="update-release-notes" style="white-space:pre-wrap;overflow-wrap:anywhere;font-size:13px;max-height:240px;overflow:auto"></p><button id="update-install" class="primary" disabled>Neue Version installieren</button></div></article><article class="card"><p class="kicker muted">Aus Datei</p><h2>Firmware auswählen</h2><p class="muted">Wähle die <strong>firmware-app-at-0x10000.bin</strong> aus diesem Projekt. Die Datei kann auch ohne Internetverbindung übertragen werden.</p><label class="field" for="update-file">Gateway-Firmware (.bin)</label><input id="update-file" type="file" accept=".bin,application/octet-stream"><button id="update-file-install" class="primary" style="margin-top:22px" disabled>Datei installieren</button><p class="info">WLAN, MQTT und Geräteauswahl bleiben erhalten. Das Gateway startet nach erfolgreicher Prüfung neu. Währenddessen Stromversorgung und Browser geöffnet lassen.</p><p class="notice">Gesamtimages, ZIP-Dateien und fremde Firmware werden nicht als Update akzeptiert. Die echte Heizungssteuerung bleibt abhängig vom passenden Treiber.</p></article></div><article class="card" style="margin-top:20px" aria-live="polite"><h2>Update-Status</h2><p id="update-progress-text">Bereit.</p><progress id="update-progress" max="100" value="0" style="width:100%;accent-color:var(--green)" aria-label="Update-Fortschritt"></progress></article></section>
<section class="page" id="page-system" hidden><div class="heading"><div><p class="kicker muted">Dein lokales Gateway</p><h1>System & Diagnose</h1><p class="muted">Einstellungen, Zustand und letzte Ereignisse.</p></div><a class="button quiet" href="/api/diagnostics" download="gateway-diagnose.json">Diagnose laden ↓</a></div><div class="grid twocol"><div class="stack"><article class="card"><h2>Persönlich einrichten</h2><form id="label-form"><label class="field" for="label">Anzeigename</label><input id="label" maxlength="48" required placeholder="Meine Dieselheizung"><div class="actions"><button class="primary" id="label-save" disabled>Namen speichern</button></div></form></article><article class="card"><h2>Gateway</h2><div class="listrow"><span>Board</span><strong>Seeed XIAO ESP32-S3</strong></div><div class="listrow"><span>Firmware</span><strong data-version>—</strong></div><div class="listrow"><span>Laufzeit</span><strong id="uptime">—</strong></div><div class="listrow"><span>Freier Arbeitsspeicher</span><strong id="heap">—</strong></div><div class="listrow"><span>PSRAM</span><strong id="psram">—</strong></div><div class="listrow"><span>IP-Adresse</span><strong id="ip">—</strong></div><div class="actions"><button class="quiet" id="reboot" disabled>Gateway neu starten</button></div></article></div><div class="stack"><article class="card"><div class="cardhead"><h2>Letzte Ereignisse</h2><span class="badge">Seit Neustart</span></div><ol class="events" id="events"><li>Wird geladen …</li></ol></article><article class="card"><h2>Ausbaustand</h2><div class="listrow"><span>Weboberfläche & Einrichtung</span><strong class="good">Verfügbar</strong></div><div class="listrow"><span>Heizungsprotokolle</span><strong class="good">8 Profile · Gerätetest ausstehend</strong></div><div class="listrow"><span>MQTT</span><strong id="system-mqtt" class="muted">Deaktiviert</strong></div><div class="listrow"><span>Zigbee</span><strong class="muted">Zusatzhardware nötig</strong></div><p class="notice">Der ESP32-S3 unterstützt WLAN und Bluetooth LE. Für Zigbee ist ein zusätzliches 802.15.4-Funkmodul erforderlich.</p></article></div></div><p class="notice">Die Weboberfläche verwendet lokales HTTP ohne Benutzeranmeldung und ist für dein vertrauenswürdiges Netzwerk vorgesehen. Der Diagnose-Download enthält keine Passwörter oder WLAN-Namen.</p></section>
<footer class="footer"><span>Diesel Heater Gateway · <span data-version>—</span></span><span id="updated">Verbindung wird hergestellt …</span></footer></main></div>
<div id="toast" class="toast" role="status" hidden></div><dialog id="confirm"><h2 id="confirm-title"></h2><p id="confirm-text"></p><div class="actions"><button id="confirm-ok" class="primary">Bestätigen</button><button id="confirm-cancel">Abbrechen</button></div></dialog>
<script>
'use strict';
const $=id=>document.getElementById(id);
let state=null, token='', online=false, polling=false, working=false, demo=false, demoOn=false, demoMode='temp', demoTemp=21, demoLevel=3;
let mqttLoaded=false,mqttConfig=null;
let updateWorking=false,updateInitialized=false,updateRelease=null,updateSession='',updateBootToken='';
let rows=[], networkRows=[], lastScanRevision=-1, lastWifiRevision=-1, lastLogSequence=-1, lastDeviceState='', initialized=false, toastTimer, confirmResolve;
const text=(id,value)=>{$(id).textContent=value;};
const duration=n=>{n=Math.max(0,Math.floor(n));return n>=86400?Math.floor(n/86400)+' T '+Math.floor(n%86400/3600)+' Std':n>=3600?Math.floor(n/3600)+' Std '+Math.floor(n%3600/60)+' Min':n>=60?Math.floor(n/60)+' Min':n+' Sek';};
function toast(message,error=false){clearTimeout(toastTimer);text('toast',message);$('toast').classList.toggle('error',error);$('toast').hidden=false;toastTimer=setTimeout(()=>$('toast').hidden=true,error?9000:5500);}
async function api(path,data){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),7000);try{let r=await fetch('/api/'+path,{method:data===undefined?'GET':'POST',cache:'no-store',signal:controller.signal,...(data===undefined?{}:{headers:{'Content-Type':'application/x-www-form-urlencoded','X-Gateway-Token':token},body:new URLSearchParams(data)})});if(!r.ok)throw Error(await r.text());return await r.json();}catch(e){if(e.name==='AbortError')throw Error('Zeitüberschreitung. Verbindung zum Gateway prüfen.');throw e;}finally{clearTimeout(timer);}}
function page(name){if(!['home','devices','wifi','mqtt','updates','system'].includes(name))name='home';document.querySelectorAll('.page').forEach(el=>el.hidden=el.id!=='page-'+name);document.querySelectorAll('[data-page]').forEach(el=>{if(el.dataset.page===name)el.setAttribute('aria-current','page');else el.removeAttribute('aria-current');});text('crumb',{home:'Übersicht',devices:'Bluetooth',wifi:'WLAN',mqtt:'MQTT',updates:'Updates',system:'System'}[name]);history.replaceState(null,'','#'+name);}
document.querySelectorAll('[data-page]').forEach(b=>b.onclick=()=>page(b.dataset.page));document.querySelectorAll('[data-go]').forEach(b=>b.onclick=()=>page(b.dataset.go));page(location.hash.slice(1));
function confirmAction(title,message,button='Bestätigen'){text('confirm-title',title);text('confirm-text',message);text('confirm-ok',button);$('confirm').showModal();return new Promise(resolve=>confirmResolve=resolve);}
function closeConfirm(value){$('confirm').close();if(confirmResolve){confirmResolve(value);confirmResolve=null;}}
$('confirm-ok').onclick=()=>closeConfirm(true);$('confirm-cancel').onclick=()=>closeConfirm(false);$('confirm').addEventListener('cancel',e=>{e.preventDefault();closeConfirm(false);});
async function action(path,data={},message='Gespeichert.'){if(working||!online)return false;working=true;renderEnabled();try{await api(path,data);if(message)toast(message);await refresh();return true;}catch(e){toast(e.message,true);return false;}finally{working=false;renderEnabled();}}
function renderEnabled(){renderUpdateEnabled();const busy=working||updateWorking||state?.update?.active||state?.update?.ready||!online,radio=busy||state?.scanning||state?.wifiScanning||state?.heater?.linked||state?.heater?.connecting;['scan','wifi-scan','forget','reboot','wifi-save'].forEach(id=>$(id).disabled=!!radio);['label-save'].forEach(id=>$(id).disabled=busy);$('mqtt-save').disabled=busy||!mqttLoaded||state?.mqtt?.changing;$('mqtt-send').disabled=busy||!state?.mqtt?.connected||state?.mqtt?.changing;$('mqtt-discover').disabled=busy||!state?.mqtt?.connected||!state?.mqtt?.discovery||state?.mqtt?.changing;document.querySelectorAll('.choose-device').forEach(b=>b.disabled=!!radio||b.dataset.address===state?.selected);renderHeater();}
function commandMessage(code){return ({queued:'Befehl vorgemerkt …',sent_awaiting_confirmation:'Gesendet · warte auf Antwort der Heizung',confirmed:'Heizung hat die Änderung bestätigt',already_in_requested_state:'Gewünschter Zustand bereits erreicht',not_confirmed_no_retry:'Änderung nicht bestätigt. Kein automatischer Wiederholungsversuch.',fresh_matching_state_required:'Bitte zuerst die passende Betriebsart wählen und ihre Bestätigung abwarten.',heater_state_unavailable:'Keine aktuellen Heizungswerte. Verbindung prüfen.',command_not_supported:'Dieser Befehl wird für das Profil nicht unterstützt.',cooldown_in_progress:'Die Heizung läuft nach. Bitte warten.',heater_reports_error:'Die Heizung meldet einen Fehler. Originalbedienteil prüfen.',start_heater_before_setting:'Bei diesem Profil zuerst bewusst einschalten, danach den Sollwert ändern.',set_heater_to_celsius_first:'Bitte die Heizung in der Hersteller-App auf Celsius stellen.',connection_lost_not_confirmed:'Verbindung verloren. Änderung nicht bestätigt.',stop_ventilation_first:'Lüften zuerst am Originalbedienteil beenden.',ble_write_failed:'Bluetooth-Befehl konnte nicht gesendet werden.'})[code]||code||'Noch kein Steuerbefehl';}
function renderLiveHeater(){
  const h=state?.heater||{},v=h.state||{},available=online&&h.available,busy=working||updateWorking||!h.controlsReady||!online;
  const mode=v.operating_mode,fan=mode==='ventilation',off=v.mode==='off',temperature=mode==='temperature';
  const show=(value,suffix='')=>available&&Number.isFinite(value)?Number(value.toFixed(1)).toLocaleString('de-DE')+suffix:'—'+suffix;
  text('heater-badge',available?'Heizung verbunden':h.connecting?'Verbindet …':'Heizung nicht verbunden');
  text('heater-subtitle',h.profileName||'Heizung einrichten');text('run-state',available?(v.error_code?'Fehler '+v.error_code:v.cooldown?'Nachlauf':fan?'Lüften':off?'Aus':'Heizbetrieb'):'Nicht verbunden');
  text('room-temp',show(v.room_temperature));text('voltage',show(v.voltage,' V'));text('body-temp',show(v.case_temperature,' °C'));text('power-level',show(v.level,' / 10'));
  text('dial-note',available?'Heizungsantwort vor '+h.age+' Sekunden':'Warte auf gültige Heizungsantwort');text('heater-note',h.status||'Heizung unter Bluetooth-Geräte einrichten.');
  text('setpoint-label',temperature?'Solltemperatur':'Leistungsstufe');text('setpoint',show(temperature?v.target_temperature:v.level));text('setpoint-unit',temperature?' °C':' / 10');
  ['mode-temp','mode-level','mode-fan','minus','plus','power'].forEach(id=>$(id).disabled=busy);
  $('mode-temp').setAttribute('aria-pressed',temperature);$('mode-level').setAttribute('aria-pressed',mode==='level');$('mode-fan').setAttribute('aria-pressed',fan);
  $('mode-temp').disabled=$('mode-level').disabled=!!(busy||fan||v.cooldown||v.error_code);
  $('mode-fan').disabled=!!(busy||!h.ventilationSupported||!off||v.cooldown||v.error_code);
  const current=temperature?v.target_temperature:v.level;
  $('minus').disabled=busy||fan||!!v.cooldown||!!v.error_code||!Number.isFinite(current)||current<=(temperature?8:1);
  $('plus').disabled=busy||fan||!!v.cooldown||!!v.error_code||!Number.isFinite(current)||current>=(temperature?35:10);
  $('power').disabled=busy||fan||!!v.cooldown;
  text('power',!available?'Heizung nicht verbunden':fan?'Lüften am Bedienteil beenden':v.cooldown?'Nachlauf abwarten':off?'Heizung einschalten':'Heizung ausschalten');
  text('control-info',fan?'Lüften läuft. Für dieses Profil ist der Ausschaltbefehl noch nicht eindeutig bestätigt; bitte am Originalbedienteil beenden.':commandMessage(h.result));
}
function renderHeater(){
  const h=state?.heater||{},busy=working||updateWorking||!online||state?.update?.active||state?.update?.ready;
  const linkBusy=h.linked||h.connecting;
  $('heater-save').disabled=busy||linkBusy;$('heater-connect').disabled=busy||linkBusy||!h.pinSet||!state?.selected||state?.scanning||state?.wifiScanning;
  $('heater-disconnect').disabled=busy||!linkBusy;text('heater-link-status',h.status||'Nicht verbunden');
  if(!demo){$('demo-banner').hidden=true;$('demo-toggle').checked=false;renderLiveHeater();return;}
  $('demo-banner').hidden=!demo;$('demo-toggle').checked=demo;
  text('heater-badge',demo?'Demo · Beispieldaten':'Heizung nicht verbunden');$('heater-badge').classList.toggle('gold',demo);
  text('heater-subtitle',demo?'Vorschau · keine echte Heizung':state?.selected?'Gerät gespeichert · Treiber noch ausstehend':'Warte auf die erste Verbindung');
  text('run-state',demo?(demoOn?(demoMode==='fan'?'Demo: Lüften':'Demo: Heizbetrieb'):'Demo: Aus'):'Nicht verbunden');$('run-state').classList.toggle('gold',demo);
  text('room-temp',demo?'18,7':'—');text('dial-note',demo?'Beispielwert · keine Live-Messung':'Noch keine Messwerte');
  text('voltage',demo?'12,4 V':'— V');text('body-temp',demo?(demoOn&&demoMode!=='fan'?'126 °C':'19 °C'):'— °C');text('power-level',demo?(demoOn?demoLevel+' / 10':'0 / 10'):'— / 10');
  text('heater-note',demo?'Demoansicht · alle Heizungswerte sind simuliert.':state?.selected?'Die Auswahl ist gespeichert. Verbindung noch nicht geprüft.':'Richte dein Gateway ein oder erkunde die Demo.');
  ['mode-temp','mode-level','mode-fan','minus','plus','power'].forEach(id=>$(id).disabled=!demo);
  $('mode-fan').setAttribute('aria-pressed',demoMode==='fan');
  $('mode-temp').setAttribute('aria-pressed',demoMode==='temp');$('mode-level').setAttribute('aria-pressed',demoMode==='level');
  text('setpoint-label',demoMode==='temp'?'Solltemperatur':demoMode==='fan'?'Lüfterstufe (Beispiel)':'Leistungsstufe');text('setpoint',demo?(demoMode==='temp'?demoTemp:demoLevel):'—');text('setpoint-unit',demoMode==='temp'?' °C':' / 10');
  text('power',demo?(demoOn?'Demo ausschalten':demoMode==='fan'?'Lüften ausprobieren':'Demo starten'):'Heizung nicht verbunden');$('power').classList.toggle('warm',demo);$('power').classList.toggle('primary',!demo);
  text('control-info',demo?'Nur Vorschau: Sollwerte und Ein/Aus ändern ausschließlich diese Demo. Nichts wird gespeichert oder an die Heizung gesendet.':'Heizungssteuerung noch nicht verfügbar. Lüften benötigt ein unterstütztes Steuergerät und einen geprüften Treiber. In der Demo kannst du den Modus ausprobieren.');
  if(demo){$('minus').disabled=demoMode==='temp'?demoTemp<=8:demoLevel<=1;$('plus').disabled=demoMode==='temp'?demoTemp>=35:demoLevel>=10;}
}
$('demo-toggle').onchange=()=>{demo=$('demo-toggle').checked;demoOn=false;renderHeater();};$('exit-demo').onclick=()=>{demo=false;demoOn=false;renderHeater();};$('mode-temp').onclick=()=>{if(!demo){sendHeater('operating_mode','temperature');return;}demoMode='temp';renderHeater();};$('mode-fan').onclick=()=>{if(!demo){sendHeater('operating_mode','ventilation');return;}demoMode='fan';demoOn=false;renderHeater();};$('mode-level').onclick=()=>{if(!demo){sendHeater('operating_mode','level');return;}demoMode='level';renderHeater();};
function step(amount){if(!demo){const s=state?.heater?.state||{},t=s.operating_mode==='temperature',value=t?s.target_temperature:s.level;if(Number.isFinite(value))sendHeater(t?'temperature':'level',String(Math.round(value)+amount));return;}if(demoMode==='temp')demoTemp=Math.max(8,Math.min(35,demoTemp+amount));else demoLevel=Math.max(1,Math.min(10,demoLevel+amount));renderHeater();}
$('minus').onclick=()=>step(-1);$('plus').onclick=()=>step(1);$('power').onclick=()=>{if(demo){demoOn=!demoOn;renderHeater();}else sendHeater('power',state?.heater?.state?.mode==='off'?'ON':'OFF');};
async function sendHeater(command,value){if(demo||!state?.heater?.controlsReady)return;await action('heater/command',{command,value},'Befehl angenommen. Bestätigung der Heizung folgt.');}
$('heater-keep-pin').onchange=()=>{$('heater-pin').disabled=$('heater-keep-pin').checked;};
$('heater-form').onsubmit=async e=>{e.preventDefault();if(await action('heater/config',{profile:$('heater-profile').value,pin:$('heater-pin').value,keepPin:$('heater-keep-pin').checked},'Profil und PIN gespeichert.')){$('heater-pin').value='';$('heater-keep-pin').checked=true;$('heater-pin').disabled=true;}};
$('heater-connect').onclick=()=>action('heater/connect',{localEpoch:String(Math.floor(Date.now()/1000)-new Date().getTimezoneOffset()*60)},'Verbindungsaufbau gestartet.');
$('heater-disconnect').onclick=()=>action('heater/disconnect',{},'Verbindung wird getrennt.');
function renderState(){if(!state)return;
  const s=state;document.querySelectorAll('[data-version]').forEach(el=>el.textContent='v'+s.version);text('gateway-name',s.label);document.title=s.label+' · Diesel Heater';
  text('net-status',online?(s.connected?'WLAN verbunden':s.ap?'Setup-Hotspot':'WLAN verbindet …'):'Gateway offline');$('net-dot').classList.toggle('off',!online||!s.connected);$('side-dot').classList.toggle('off',!online);text('side-status',online?'Gateway erreichbar':'Gateway offline');
  text('home-wifi',online?(s.connected?s.ssid:s.ap?'Setup-Hotspot':'Verbindet …'):'Gateway offline');text('home-web',online?'Lokal erreichbar':'Nicht erreichbar');text('home-heater',s.heater?.available?'Verbunden · Live-Werte':s.heater?.status||'Nicht verbunden');
  text('next-title',s.selected?'Bereit für den nächsten Test':'Deine Heizung hinzufügen');text('next-text',s.selected?'Wähle unter Bluetooth-Geräte das passende Protokoll, speichere die PIN und verbinde dich. Danach werden echte Messwerte angezeigt.':'Wenn die Heizung in Reichweite ist: mit Strom versorgen, die Hersteller-App schließen und nach Bluetooth-Geräten suchen.');
  $('saved-card').hidden=!s.selected;text('saved-name',s.name||'Unbenanntes Gerät');text('saved-address',s.selected);text('saved-hint','Protokollhinweis: '+(s.hint||'Unbekannt')+'. Gespeichert bedeutet noch nicht verbunden.');
  text('scan',s.scanning?'Suche läuft …':'Geräte suchen');text('scan-status',s.scanning?'Suche läuft etwa 8 Sekunden. Vorherige Ergebnisse bleiben sichtbar.':s.hasScan?'Letzte Suche vor '+duration(s.scanAge)+(s.deviceCount>=80?' · Ergebnislimit erreicht.':''):'Noch keine Suche durchgeführt.');text('device-count',s.deviceCount+' Geräte');
  text('wifi-current',s.connected?'Verbunden mit '+s.ssid+' · '+s.rssi+' dBm':'Noch nicht mit einem WLAN verbunden.');text('wifi-scan',s.wifiScanning?'Sucht …':'Suchen');text('ap-state',s.ap?'Aktiv':'Ausgeschaltet');
  text('uptime',duration(s.uptime));text('heap',Math.round(s.freeHeap/1024)+' KB');text('psram',Math.round(s.psram/1048576)+' MB');text('ip',s.ip||s.apIp||'—');
  if(!initialized){$('heater-profile').value=s.heater?.profile>=0?String(s.heater.profile):'';$('heater-keep-pin').checked=!!s.heater?.pinSet;$('heater-pin').disabled=!!s.heater?.pinSet;$('ssid').value=s.ssid;$('label').value=s.label;$('keep-password').checked=!!s.ssid;initialized=true;updatePassword();}
  const key=[s.selected,s.scanning,s.wifiScanning,online].join('|');if(key!==lastDeviceState){lastDeviceState=key;renderDevices();}
  renderEnabled();renderHeater();renderMqtt();renderUpdateState();
}
function empty(target,title,body){const box=document.createElement('div');box.className='empty';const b=document.createElement('b'),p=document.createElement('p');b.textContent=title;p.textContent=body;box.append(b,p);$(target).replaceChildren(box);}
function renderDevices(){
  const query=$('search').value.trim().toLowerCase(), only=$('only-heaters').checked;
  const filtered=rows.filter(d=>(!only||d.hint!=='Unbekannt')&&((d.name||'')+' '+d.address).toLowerCase().includes(query)).sort((a,b)=>Number(b.hint!=='Unbekannt')-Number(a.hint!=='Unbekannt')||b.rssi-a.rssi);
  if(!filtered.length){empty('devices',!state?.hasScan?'Bereit für die erste Suche':'Keine passenden Geräte',!state?.hasScan?'Starte die Bluetooth-Suche, wenn deine Heizung in der Nähe ist.':rows.length?'Ändere den Suchbegriff oder deaktiviere den Heizungsfilter.':'Heizung mit Strom versorgen, Hersteller-App schließen und erneut suchen.');return;}
  const fragment=document.createDocumentFragment();for(const d of filtered){const row=document.createElement('div');row.className='device';const info=document.createElement('div'),title=document.createElement('h3'),detail=document.createElement('p'),badge=document.createElement('span'),button=document.createElement('button');title.textContent=d.name||'Unbenanntes Gerät';detail.textContent=d.address+' · '+d.rssi+' dBm';badge.className='badge'+(d.hint!=='Unbekannt'?' gold':'');badge.textContent=d.hint==='Unbekannt'?'Kein Protokollhinweis':'Mögliche Heizung · '+d.hint;info.append(title,detail,badge);if(d.services?.length){const details=document.createElement('details'),summary=document.createElement('summary'),code=document.createElement('code');summary.textContent='Service-UUIDs';code.textContent=d.services.join(', ');details.append(summary,code);info.append(details);}button.className='small choose-device';button.dataset.address=d.address;button.textContent=d.address===state?.selected?'Gespeichert':'Auswählen';button.disabled=working||!online||state?.scanning||state?.wifiScanning||d.address===state?.selected;button.onclick=async()=>{if(await confirmAction('Gerät speichern?',(d.name||'Unbenanntes Gerät')+'\n'+d.address+'\n\n'+(d.hint==='Unbekannt'?'Dieses Gerät hat keinen bekannten Heizungs-Protokollhinweis. Bitte nur auswählen, wenn es deine Heizung ist.':'Der Protokollhinweis muss später noch an der Heizung geprüft werden.'),'Gerät speichern'))await action('select',{address:d.address},'Gerät gespeichert. Noch keine Verbindung hergestellt.');};row.append(info,button);fragment.append(row);} $('devices').replaceChildren(fragment);
}
$('search').oninput=renderDevices;$('only-heaters').onchange=renderDevices;
$('scan').onclick=async()=>{if(await action('scan',{},'Bluetooth-Suche gestartet.')){state.scanning=true;renderState();}};
$('forget').onclick=async()=>{if(await confirmAction('Auswahl aufheben?','Die gespeicherte Geräteauswahl wird entfernt. Du kannst das Gerät später über die Suche erneut auswählen.','Auswahl aufheben'))await action('forget',{},'Geräteauswahl aufgehoben.');};
function updatePassword(){const same=state&&$('ssid').value===state.ssid&&!!state.ssid;$('keep-password').disabled=!same;if(!same)$('keep-password').checked=false;const disabled=$('keep-password').checked||$('open-network').checked;$('password').disabled=disabled;$('password').required=!disabled;$('password').placeholder=$('keep-password').checked?'Gespeichertes Passwort bleibt erhalten':'Passwort eingeben';}
$('ssid').oninput=updatePassword;$('keep-password').onchange=()=>{if($('keep-password').checked)$('open-network').checked=false;updatePassword();};$('open-network').onchange=()=>{if($('open-network').checked)$('keep-password').checked=false;updatePassword();};
function renderNetworks(){if(!networkRows.length){empty('networks','Keine Netzwerke gefunden','Du kannst einen WLAN-Namen auch direkt eingeben.');return;}const fragment=document.createDocumentFragment();for(const n of networkRows){const row=document.createElement('div'),info=document.createElement('div'),title=document.createElement('strong'),p=document.createElement('p'),button=document.createElement('button');row.className='network';title.textContent=n.ssid;p.textContent=n.rssi+' dBm · '+(n.open?'Offen':'Passwortgeschützt');button.type='button';button.className='small quiet';button.textContent='Übernehmen';button.onclick=()=>{$('ssid').value=n.ssid;$('keep-password').checked=n.ssid===state?.ssid&&!n.open;$('open-network').checked=n.open;updatePassword();if(!$('password').disabled)$('password').focus();toast('WLAN übernommen. Zum Verbinden speichern.');};info.append(title,p);row.append(info,button);fragment.append(row);}$('networks').replaceChildren(fragment);}
$('wifi-scan').onclick=async()=>{if(await action('wifi-scan',{},'WLAN-Suche gestartet.')){state.wifiScanning=true;renderState();}};
$('wifi-form').onsubmit=async e=>{e.preventDefault();if(await confirmAction('WLAN speichern?','Das Gateway startet neu und verbindet sich mit „'+$('ssid').value+'“. Du erreichst es danach im gewählten Netzwerk.','Speichern & neu starten')){if(await action('wifi',{ssid:$('ssid').value,password:$('password').value,keepPassword:$('keep-password').checked,open:$('open-network').checked},'Gespeichert. Gateway startet neu. Bei Bedarf ins neue WLAN wechseln.'))$('password').value='';}};
$('label-form').onsubmit=async e=>{e.preventDefault();await action('settings',{label:$('label').value},'Anzeigename gespeichert.');};
$('reboot').onclick=async()=>{if(await confirmAction('Gateway neu starten?','Die Weboberfläche ist kurz nicht erreichbar. Deine Einstellungen bleiben gespeichert.','Neu starten'))await action('reboot',{},'Neustart angefordert. Verbindung wird wiederhergestellt.');};
function renderEvents(events){const fragment=document.createDocumentFragment();for(const e of [...events].reverse()){const li=document.createElement('li'),time=document.createElement('time'),message=document.createElement('span');time.textContent=duration(e.at);message.textContent=e.message;li.append(time,message);fragment.append(li);}$('events').replaceChildren(fragment);}
async function refresh(){if(polling)return;polling=true;try{const s=await api('status');if(state&&s.token!==token){lastScanRevision=-1;lastWifiRevision=-1;lastLogSequence=-1;}state=s;token=s.token;online=true;$('offline').hidden=true;renderState();text('updated','Aktualisiert um '+new Date().toLocaleTimeString('de-DE',{hour:'2-digit',minute:'2-digit',second:'2-digit'}));if(!mqttLoaded&&!s.mqtt?.changing)await loadMqttConfig();if(s.scanRevision!==lastScanRevision){rows=await api('devices');lastScanRevision=s.scanRevision;renderDevices();}if(s.wifiRevision!==lastWifiRevision){networkRows=await api('networks');lastWifiRevision=s.wifiRevision;if(s.wifiHasScan)renderNetworks();else empty('networks','Bereit für die WLAN-Suche','Suche nach WLANs oder trage den Namen direkt ein.');}if(s.logSequence!==lastLogSequence){renderEvents(await api('events'));lastLogSequence=s.logSequence;}}catch(e){online=false;$('offline').hidden=false;renderState();renderEnabled();text('updated','Verbindung unterbrochen · erneuter Versuch läuft');}finally{polling=false;}}

async function loadMqttConfig(){mqttConfig=await api('mqtt');mqttLoaded=true;$('mqtt-enabled').checked=mqttConfig.enabled;$('mqtt-discovery').checked=mqttConfig.discovery;$('mqtt-host').value=mqttConfig.host;$('mqtt-port').value=mqttConfig.port;$('mqtt-user').value=mqttConfig.username;$('mqtt-interval').value=mqttConfig.interval;$('mqtt-keep').checked=mqttConfig.passwordSet;$('mqtt-password').value='';updateMqttPassword();renderEnabled();}
function updateMqttPassword(){const same=mqttConfig&&$('mqtt-host').value.trim()===mqttConfig.host&&Number($('mqtt-port').value)===mqttConfig.port&&$('mqtt-user').value===mqttConfig.username&&mqttConfig.passwordSet;$('mqtt-keep').disabled=!same;if(!same)$('mqtt-keep').checked=false;$('mqtt-password').disabled=$('mqtt-keep').checked;$('mqtt-password').placeholder=$('mqtt-keep').checked?'Gespeichertes Passwort bleibt erhalten':'Passwort eingeben oder leer lassen';}
['mqtt-host','mqtt-port','mqtt-user'].forEach(id=>$(id).oninput=updateMqttPassword);$('mqtt-keep').onchange=updateMqttPassword;
function renderMqtt(){const m=state?.mqtt;if(!m)return;const status=!online?'Gateway offline':m.changing?'Wird übernommen …':!m.enabled?'Deaktiviert':m.connected?'Verbunden':state.connected?'Verbindung wird aufgebaut …':'Warte auf WLAN';text('mqtt-badge',status);text('mqtt-connection',status);text('home-mqtt',status);text('system-mqtt',m.enabled?'Eingerichtet':'Deaktiviert');text('mqtt-sent',m.sent);text('mqtt-acks',m.acknowledged);text('mqtt-received',m.received);text('mqtt-discovered',m.discovery&&m.connected?m.discoveryAcknowledged+' / '+m.discoveryTotal:'—');$('mqtt-error').hidden=!m.enabled||!m.lastError;text('mqtt-error',m.lastError||'');text('mqtt-result',m.lastResult?'Letzter Befehl: '+m.lastResult:'Noch kein Befehl empfangen.');$('mqtt-topic').value=m.baseTopic;}
$('mqtt-form').onsubmit=async e=>{e.preventDefault();if(await action('mqtt',{enabled:$('mqtt-enabled').checked,discovery:$('mqtt-discovery').checked,host:$('mqtt-host').value,port:$('mqtt-port').value,username:$('mqtt-user').value,password:$('mqtt-password').value,keepPassword:$('mqtt-keep').checked,interval:$('mqtt-interval').value},'MQTT-Konfiguration wird übernommen.')){$('mqtt-password').value='';mqttLoaded=false;renderEnabled();await refresh();}};
$('mqtt-send').onclick=()=>action('mqtt/publish',{},'Status für den Broker eingereiht.');$('mqtt-discover').onclick=()=>action('mqtt/discovery',{},'Home-Assistant-Erkennung wird erneut gesendet.');
// SHA-256 is also needed on plain HTTP LAN pages where Web Crypto is unavailable.
function firmwareSha256(bytes){
  const rotate=(x,n)=>(x>>>n)|(x<<(32-n));
  const k=[],initial=[];
  for(let n=2;k.length<64;n++){
    let prime=true;for(let d=2;d*d<=n;d++)if(n%d===0){prime=false;break;}
    if(prime){if(initial.length<8)initial.push((Math.sqrt(n)%1*4294967296)|0);k.push((Math.cbrt(n)%1*4294967296)|0);}
  }
  const data=new Uint8Array(Math.ceil((bytes.length+9)/64)*64);data.set(bytes);data[bytes.length]=128;
  const view=new DataView(data.buffer);view.setUint32(data.length-8,Math.floor(bytes.length/536870912));view.setUint32(data.length-4,(bytes.length*8)>>>0);
  const h=initial.slice(),w=new Int32Array(64);
  for(let offset=0;offset<data.length;offset+=64){
    for(let i=0;i<16;i++)w[i]=view.getInt32(offset+i*4);
    for(let i=16;i<64;i++){const a=w[i-15],b=w[i-2];w[i]=(w[i-16]+(rotate(a,7)^rotate(a,18)^(a>>>3))+w[i-7]+(rotate(b,17)^rotate(b,19)^(b>>>10)))|0;}
    let [a,b,c,d,e,f,g,hh]=h;
    for(let i=0;i<64;i++){const t1=(hh+(rotate(e,6)^rotate(e,11)^rotate(e,25))+((e&f)^(~e&g))+k[i]+w[i])|0;const t2=((rotate(a,2)^rotate(a,13)^rotate(a,22))+((a&b)^(a&c)^(b&c)))|0;hh=g;g=f;f=e;e=(d+t1)|0;d=c;c=b;b=a;a=(t1+t2)|0;}
    [a,b,c,d,e,f,g,hh].forEach((v,i)=>h[i]=(h[i]+v)|0);
  }
  return h.map(x=>(x>>>0).toString(16).padStart(8,'0')).join('');
}
function firmwareVersion(value){const match=/^v?(0|[1-9]\d*)\.(0|[1-9]\d*)\.(0|[1-9]\d*)$/.exec(value);return match?match.slice(1).map(Number):null;}
function newerFirmware(next,current){const a=firmwareVersion(next),b=firmwareVersion(current);if(!a||!b)return false;for(let i=0;i<3;i++)if(a[i]!==b[i])return a[i]>b[i];return false;}
function validateFirmwareManifest(m,tag,maxSize){
  if(m.schema!==1||m.project!=='diesel-heater-gateway'||m.target!=='xiao_esp32s3'||m.firmwareId!=='DIESELHEATER_GATEWAY:XIAO_ESP32S3:OTA1'||m.file!=='firmware-app-at-0x10000.bin'||!firmwareVersion(m.version)||tag!=='v'+m.version||!Number.isInteger(m.size)||m.size<1024||m.size>maxSize||!/^[a-f0-9]{64}$/.test(m.sha256))throw Error('Die Veröffentlichung enthält kein passendes Gateway-Update.');
  return m;
}
function renderUpdateState(){
  const u=state?.update;if(!u)return;
  if(!updateInitialized){$('update-repo').value=u.repository;updateInitialized=true;}
  if(!updateWorking&&u.active){text('update-progress-text','Übertragung in einem anderen Browser: '+Math.floor(100*u.received/u.size)+' %');$('update-progress').value=100*u.received/u.size;}
}
function renderUpdateEnabled(){
  const busy=working||updateWorking||!online||state?.update?.active||state?.update?.ready;
  ['update-repo-save','update-repo','update-file'].forEach(id=>$(id).disabled=!!busy);
  $('update-check').disabled=!!busy||!state?.update?.repository;
  $('update-file-install').disabled=!!busy||!$('update-file').files?.length;
  $('update-install').disabled=!!busy||!updateRelease||!newerFirmware(updateRelease.version,state?.version||'');
}
async function updateFetch(url,options={},timeout=20000){
  const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),timeout);
  try{const r=await fetch(url,{cache:'no-store',credentials:'omit',...options,signal:controller.signal});if(!r.ok){if(r.status===404&&url.startsWith('https:'))throw Error('Kein öffentliches Release gefunden. Repository und Veröffentlichungen prüfen.');if(r.status===403&&url.startsWith('https:'))throw Error('GitHub-Anfragelimit erreicht oder Zugriff verweigert. Später erneut versuchen.');throw Error(url.startsWith('/api/')?await r.text():'Download fehlgeschlagen (HTTP '+r.status+').');}return r;}
  catch(e){if(e.name==='AbortError')throw Error('Zeitüberschreitung. Verbindung prüfen und erneut versuchen.');throw e;}finally{clearTimeout(timer);}
}
$('update-repo-form').onsubmit=async e=>{e.preventDefault();if(await action('update/repository',{repository:$('update-repo').value},'Update-Repository gespeichert.')){$('update-repo').value=state.update.repository;updateRelease=null;$('update-release').hidden=true;text('update-release-status','Repository gespeichert. Jetzt nach Updates suchen.');renderEnabled();}};
$('update-file').onchange=renderEnabled;
$('update-check').onclick=async()=>{
  if(updateWorking||working)return;updateWorking=true;updateRelease=null;$('update-release').hidden=true;renderEnabled();text('update-release-status','GitHub wird geprüft …');
  try{
    const repo=state.update.repository;
    const release=await(await updateFetch('https://api.github.com/repos/'+repo+'/releases/latest')).json();
    if(release.draft||release.prerelease||!/^v\d+\.\d+\.\d+$/.test(release.tag_name))throw Error('Keine passende stabile Version veröffentlicht.');
    const base='https://raw.githubusercontent.com/'+repo+'/'+encodeURIComponent(release.tag_name)+'/';
    const m=validateFirmwareManifest(await(await updateFetch(base+'update-manifest.json')).json(),release.tag_name,state.update.maxSize);
    updateRelease={...m,url:base+m.file};text('update-release-title','Version '+m.version);text('update-release-notes',(release.body||'Keine Versionshinweise vorhanden.').slice(0,6000));$('update-release').hidden=false;
    text('update-release-status',newerFirmware(m.version,state.version)?'Neue Version verfügbar: '+state.version+' → '+m.version:m.version===state.version?'Dein Gateway ist aktuell.':'Deine installierte Version ist neuer als die veröffentlichte Version.');
  }catch(e){text('update-release-status',e.message);toast(e.message,true);}finally{updateWorking=false;renderEnabled();}
};
async function installFirmware(bytes,checksum,expectedVersion=''){
  if(bytes.length<1024||bytes.length>state.update.maxSize||bytes[0]!==0xe9||bytes[12]!==9||bytes[13]!==0||bytes[32]!==0x32||bytes[33]!==0x54||bytes[34]!==0xcd||bytes[35]!==0xab)throw Error('Bitte eine passende ESP32-S3-App-Datei auswählen; kein Gesamtimage oder ZIP.');
  if(firmwareSha256(bytes)!==checksum)throw Error('Die Prüfsumme des Downloads stimmt nicht. Update wurde nicht gestartet.');
  updateBootToken=token;const start=await api('update/start',{size:bytes.length,sha256:checksum});updateSession=start.session;
  const headers={'X-Gateway-Token':token,'X-Update-Id':updateSession};
  try{
    for(let offset=0;offset<bytes.length;offset+=start.chunkSize){
      const part=bytes.slice(offset,offset+start.chunkSize),form=new FormData();form.append('firmware',new Blob([part]),'chunk.bin');
      const response=await(await updateFetch('/api/update/chunk',{method:'POST',headers:{...headers,'X-Update-Offset':String(offset)},body:form})).json();
      if(response.received!==offset+part.length)throw Error('Übertragung nicht bestätigt. Bitte erneut starten.');
      const percent=Math.floor(100*response.received/bytes.length);$('update-progress').value=percent;text('update-progress-text','Firmware übertragen: '+percent+' %');
    }
    text('update-progress-text','Firmware wird geprüft …');
    await updateFetch('/api/update/finish',{method:'POST',headers});updateSession='';
    text('update-progress-text','Firmware geprüft. Gateway startet neu …');
    const until=Date.now()+60000;let confirmed=false;
    while(Date.now()<until){await new Promise(resolve=>setTimeout(resolve,2000));await refresh();if(online&&token!==updateBootToken){confirmed=true;break;}}
    if(!confirmed)throw Error('Übertragung erfolgreich, Neustart noch nicht bestätigt. Bitte Verbindung prüfen und Seite neu laden.');
    if(expectedVersion&&state.version!==expectedVersion)throw Error('Gateway erreichbar, aber erwartete Version nicht gestartet. Aktuell: '+state.version);
    text('update-progress-text','Update abgeschlossen. Gateway läuft mit Version '+state.version+'.');toast('Update erfolgreich. Einstellungen erhalten.');
  }catch(e){
    if(updateSession){try{await updateFetch('/api/update/cancel',{method:'POST',headers},5000);}catch(_){}updateSession='';}
    throw e;
  }
}
async function runFirmwareInstall(loader,label,expectedVersion=''){
  if(updateWorking||working||!online)return;
  if(!await confirmAction('Firmware installieren?',label+'\n\nDas Gateway startet danach neu. Deine Einstellungen bleiben erhalten. Stromversorgung und Browser geöffnet lassen.','Installieren'))return;
  updateWorking=true;$('update-progress').value=0;renderEnabled();
  try{text('update-progress-text','Datei wird geladen und geprüft …');const {bytes,checksum}=await loader();await installFirmware(bytes,checksum,expectedVersion);}
  catch(e){text('update-progress-text',e.message);toast(e.message,true);}finally{updateWorking=false;await refresh();renderEnabled();}
}
$('update-file-install').onclick=()=>{const file=$('update-file').files[0];if(!file)return;if(file.size>state.update.maxSize){toast('Firmwaredatei ist zu groß.',true);return;}runFirmwareInstall(async()=>{const bytes=new Uint8Array(await file.arrayBuffer());return {bytes,checksum:firmwareSha256(bytes)};},'Datei: '+file.name);};
$('update-install').onclick=()=>{const r=updateRelease;if(!r||!newerFirmware(r.version,state.version))return;runFirmwareInstall(async()=>{const response=await updateFetch(r.url,{},60000);const bytes=new Uint8Array(await response.arrayBuffer());if(bytes.length!==r.size)throw Error('Downloadgröße stimmt nicht mit der Veröffentlichung überein.');return {bytes,checksum:r.sha256};},'GitHub-Version '+r.version,r.version);};
window.addEventListener('beforeunload',e=>{if(updateWorking){e.preventDefault();e.returnValue='';}});

renderHeater();renderDevices();refresh();setInterval(refresh,3000);
</script></body></html>)HTML";
